public class LogicalOperators {
    public static void utility(boolean a, boolean b) {
        boolean p = a && b;
        boolean q = a || b;
        boolean r = !a;

        System.out.println(p + " " + q + " " + r);
    }
}
